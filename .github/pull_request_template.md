<!-- Every section is required. PRs that leave a section blank or as a
     placeholder will be closed without review. Keep it to one logical
     change — split unrelated changes into separate PRs. -->

## Problem

What specific problem did you hit? Describe the actual failure, error, or
gap — what you ran, what happened, what you expected. "Could be improved"
or "my tool flagged this" is not a problem statement.

## Change

What this PR does, and why this approach.

## Testing

- [ ] `node test/harness.mjs` passes
- [ ] `bash test/check.sh` passes
- [ ] `bash test/run-mock-checks.sh` passes
- [ ] If any relay agent changed: ran the mock pipeline in `test/SMOKE.md`
      (CI cannot execute relay prose — this is the only execution check)

Paste the relevant output:

```
```

## Scope

- [ ] One logical change (unrelated changes are split into separate PRs)
- [ ] Model IDs, if touched, live only in the workflow `CONFIG` block / agent
      directive defaults
- [ ] SANDBOX / MODE capability directives, if touched, remain strictly
      allow-listed at both the workflow and relay layers

## Disclosure

Was any part of this PR generated by an AI agent? If so, which model/harness?
(Not disqualifying — we just weigh agent-generated changes differently.)
